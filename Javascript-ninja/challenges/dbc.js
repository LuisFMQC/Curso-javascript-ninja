(function(){
  'use strict';

  function squareFigures(num){
    var count = 1
    var calc = [''];
    var int;
    for(var i = 1; calc[0].length < num; i++){
      calc.push[0](Math.pow(i, 2).toString);
      int = calc[0] / i;
      }
    return int;
  }

  console.log(squareFigures(3));
})();
